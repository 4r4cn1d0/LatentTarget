# GPU-pod-only extras, on top of requirements.txt.
# Official V6 focal runs additionally require CPython 3.12, device_map="auto",
# one visible NVIDIA A100 80GB (compute capability 8.0), CUDA, and bfloat16.
# The runtime probe records the exact Python patch, NumPy, sentencepiece, CUDA
# build/runtime, and hardware values at pool screening; later V6 stages must
# match that full observation. Nothing here is needed for tests or analysis.
torch==2.9.1
# Qwen's multimodal processor imports these even for text-only prompts. Keep
# their release family aligned with torch to avoid loading template-provided
# binary extensions compiled for a different PyTorch ABI.
torchvision==0.24.1
torchaudio==2.9.1
transformers==5.16.1
accelerate==1.14.0
sentencepiece>=0.2

# Continuing to a bounded conclusion

The user asked to keep working until a conclusion. I selected one final
presentation diagnostic rather than a sequence conditioned on finding success.
The plan is in `PRESENTATION_DIAGNOSTIC_PLAN_20260909.md`.

GSD resume initialization succeeded. It found no `.planning` project, state,
roadmap or interrupted agent. This is an established project with detailed
frozen packets and work logs, so starting a new project questionnaire would
discard relevant continuity. I used those records as the fallback and kept
this explicit continuation log. No historical planning files were invented.

The experimental design skill guided matched history renderings, candidate
rotations, concurrent controls and bundle level analysis. The statistical
analysis skill guides complete descriptive reporting without turning this
followup into a confirmatory result.

I inspected the provider's chat template, constrained token trie, random seed
handling and strict output checks. No concrete decoding bug was found. The
provider and watchdog tests passed all 23 cases in 0.09 seconds. This is a
code and test audit, not a substitute for the real direct answer controls.
One read only inspection command had a parenthesis typo and was corrected;
it created no output artifact and changed no experimental input.

The existing raw records, primary analyzer and prior result verdicts are not
being edited. Paid execution, results and shutdown details will be appended
after they occur. At this entry no new GPU has been started.

## Frozen implementation and local verification

The plan fixes 36 direct checks, 864 binding choices and 72 lookup checks.
The package SHA256 is
`dec89b900611e713d0209a2f813f8938b05f77071cb44992431a87b9184a9055`.
The deployment archive SHA256 is
`9f929696f3429fcc507dbd205e7d12d086a1dadd30c11d8eea581c85ed215037`.
All analyst targets and candidate vectors are in a separate local key. Only
the exact system and user strings reach the provider.

The first renderer check caught an extra blank line at the original history
boundary. I corrected the delimiter before packaging. Tests now compare every
prose prompt against the existing renderer and reconstruct all visible event
fields from both compact label assignments. No model output existed when
this was fixed. Imports needed only during local construction are deferred,
so the deployment does not include unrelated analysis modules.

The complete mock run produced 972 records, explicitly marked mock. Its poor
direct check answers are expected from the hash based fixture, not model
observations. Mock mode deliberately bypasses the real semantic control gate
so all logging and analysis paths can be exercised. A separate simulated
provider test verified that real mode stops after the 36 direct controls if
any are wrong, without making a focal call. That fixture exists only inside
a temporary test directory.

All 45 focused tests passed in 3.79 seconds, including missing and invalid
outputs, scope checks, no overwrite, provider failures, independent scalar
versus vectorized scoring, export and safe retrieval. The mock analyzer labels
its result `MOCK_NOT_SCIENTIFIC` and rejects mock results without explicit
opt in. A full local regression is running separately.

The new CPU tokenizer check initially used the minimal research environment,
which does not contain Transformers. That attempt failed before writing an
artifact. I located the existing isolated tokenizer runtime rather than
changing the research environment. The input checks are being run there.

The reporting contract and exact analyzer hash were written before any new
real output. No p values, confidence intervals or retrospective gate passes
will be added based on outcomes. Read only RunPod access confirmed that the
extension pod is stopped, has no network volume, and still quotes $1.59/hour.

## CPU check and deployment attempt

The isolated tokenizer runtime checked all 972 prompts successfully. Direct
checks use 45 to 46 tokens. Focal prose uses 5,061 to 5,090 tokens and compact
history uses 2,332 to 2,353. Lookup prompts use 4,743 to 4,782 tokens in prose
and 2,015 to 2,039 in compact form. No weights were loaded and no model calls
were made by this check. The expected no PyTorch warning in this isolated
tokenizer environment is not a GPU runtime failure.

The attempt to resume `i0szqs87ifg0id` returned HTTP 500. The scoped watchdog
and a separate status request confirmed `EXITED`, no public IP and no SSH
mapping. No model call was made. The cause of the API error is unknown. The
resume control process was closed after the guard ended.

A live A100 query reported low stock, a secure rate of $1.59/hour and a lowest
listed offer of $1.39/hour. The conservative quote used for approval remains
$1.59/hour. The plan permits one fresh pod fallback; a new scoped controller
will attempt that once. The packet and analyzer hashes are unchanged.

## Fresh pod and final local regression

The one fresh fallback created pod `itwjovb87fl9wy`, named
`latenttarget-presentation-20260909`, at 21:37:18.667 UTC on 8 September.
RunPod quotes $1.59/hour. It has one A100-SXM4-80GB, a 150 GB pod volume,
20 GB container disk and no network volume. Its exact watchdog deadline is
23:07:17.766051 UTC. The guard reported armed and alive before upload.

SSH confirmed 81,920 MiB GPU memory and `/workspace` as a mountpoint. The
frozen archive, bootstrap, exporter and scoped approval were uploaded to
`/workspace/latenttarget-presentation-20260909`. The SSH public key is the
only access key supplied to the pod; the API credential stays in the local
controller process. No previous remote result directory was overwritten.

The local full repository suite passed all 1,084 tests in 538.73 seconds.
The final precollection checks also found no credential pattern in the new
source, packet or logs, and `git diff --check` passed. The deployment archive
hash matched on the remote host before extraction. Bootstrap and collection
were then started exactly once. Startup and deployment time count toward the
runtime budget, not only generation time.

The first remote shell stopped during extraction, before bootstrap ran. This
host's workspace filesystem rejected restoring the archive's Mac UID/GID
ownership metadata. GNU tar returned exit code 2. No runtime was installed,
model loaded or model request made by that attempt. I repeated extraction of
the same verified archive with `--no-same-owner`, preserving file contents
and using the pod's current ownership. This is a deployment correction, not
a scientific rerun. The bootstrap is required to verify the package again
before any model call. Its actual start follows this correction.

## Literature review during fixed collection

The model passed all 36 direct answer controls. Collection continued into the
prespecified binding and lookup requests. No prompts, seeds, stopping rules
or primary analysis were changed after observing that gate.

The user asked to compare our methods with other papers. I used the paper
lookup skill, verified ten arXiv metadata records with the supplied parser,
and inspected relevant primary methods in nine papers, including three from
2026. The review and search log distinguish screened abstracts from methods
read, and record failed HTML and PDF accesses. No paid research service was
used. See `LITERATURE_METHODS_REVIEW_20260909.md`.

The experimental design skill guided one free, isolated mathematical check:
can equal immediate response vectors conceal different later predictions?
The new check enumerated 105 belief pairs, with 20 witnesses in a proposed
kernel and zero in two negative controls. It also checked that the example
beliefs can arise from informative earlier observations. All 14 unit tests
passed in 0.28 seconds. This is not an LLM experiment, does not change the
current simulator, and is marked `OFFLINE_DESIGN_CERTIFICATE_NOT_MODEL_EVIDENCE`.

The combined presentation, retrieval, export, watchdog and mathematical check
suite then passed 43 tests in 4.69 seconds. The earlier complete suite remains
1,084 tests; I do not describe that earlier run as including the 14 newly added
mathematical tests. The primary analyzer and plan still match their frozen
SHA256 values. A separate postcollection audit script was prepared to verify
execution order, completion hashes and CPU versus GPU input counts. It adds no
new metric or model request and will be run only after results are retrieved.
